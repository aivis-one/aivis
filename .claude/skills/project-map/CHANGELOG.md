# Changelog

## v1.0.0 (2026-04-05)

### Added
- SKILL.md with triggers, invariants, workflow (scan -> generate -> validate)
- P1 scan protocol: 7-step backend + frontend + spec scanning
- P2 generate protocol: HTML with L0/L1/L2 depth, Mermaid, filters
- P3 validate protocol: dead-end analysis, coverage, prod-readiness
- scanner-patterns.md: regex patterns + semantic screen-endpoint mapping (35 screens, 12 flows)
- status-model.md: 4-state classification + scoring formulas
- html-template.md: CSS tokens, JS architecture, card/table templates
- Output: manifest.yaml + index.html (generated at runtime)
